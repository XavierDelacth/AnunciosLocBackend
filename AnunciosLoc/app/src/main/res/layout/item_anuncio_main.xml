<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:background="@android:color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp">

        <!-- Imagem -->
        <ImageView
            android:id="@+id/img_anuncio"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@drawable/espaco_image"
            android:scaleType="centerCrop"
            app:cardCornerRadius="12dp" />

        <Space android:layout_width="12dp" android:layout_height="1dp" />

        <!-- Conteúdo -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_titulo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Apartamento T2 para Arrendar no Centro"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@color/verde_principal" />

            <!-- DESCRIÇÃO CORTADA -->
            <TextView
                android:id="@+id/tv_descricao"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Apartamento T2 mobilado com vista para o mar, perto do Largo da Independência..."
                android:textSize="14sp"
                android:textColor="@color/cinza_texto"
                android:maxLines="2"
                android:ellipsize="end" />

        </LinearLayout>

        <!-- Botões de ação (Editar e Excluir) -->
        <LinearLayout
            android:id="@+id/layoutAcoes"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Botão Editar -->
            <ImageView
                android:id="@+id/btnEditarAnuncio"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@drawable/ic_edit"
                android:padding="6dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                app:tint="@color/verde_principal"
                android:contentDescription="Editar local" />

            <!-- Botão Excluir -->
            <ImageView
                android:id="@+id/btnExcluirAnuncio"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@drawable/ic_delete"
                android:padding="6dp"
                android:layout_marginStart="4dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                app:tint="@color/vermelho_cancelar"
                android:contentDescription="Excluir local" />
        </LinearLayout>

    </LinearLayout>
</androidx.cardview.widget.CardView>