<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="@color/branco">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp">

        <!-- Imagem à esquerda -->
        <ImageView
            android:id="@+id/img_anuncio"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:scaleType="centerCrop"
            android:src="@drawable/espaco_image"
            app:cardCornerRadius="12dp" />

        <Space
            android:layout_width="12dp"
            android:layout_height="1dp" />

        <!-- Título + Descrição -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_titulo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Vendemos terreno no Sun City"
                android:textColor="@color/preto_texto"
                android:textSize="16sp"
                android:textStyle="bold"
                android:maxLines="2"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/tv_descricao"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Vendo 5 clippers no seu voo sem acidentes..."
                android:textColor="@color/cinza_texto"
                android:textSize="14sp"
                android:maxLines="2"
                android:ellipsize="end" />
        </LinearLayout>

        <!-- Ícone de guardar (bookmark) -->
        <ImageView
            android:id="@+id/btn_salvar"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/bookmark_selector"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:layout_gravity="top"
            android:layout_marginTop="50dp"
            android:clickable="true"
            android:focusable="true"
            android:contentDescription="Salvar anúncio" />

    </LinearLayout>
</androidx.cardview.widget.CardView>